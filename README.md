# supabase-js

This package exposes the library build and a small generator CLI.

## CLI usage

Run the packaged CLI with `npx`:

```bash
npx @asghonim/supabase-js add accounts
```

The `add` command copies the selected template files into the current directory and refuses to overwrite existing files.
